Front-end React (example)
- Copia estos archivos a `resources/js/` de tu proyecto Laravel + Breeze/React o úsalos en un proyecto React separado.
- Ejecuta `npm install` y `npm run dev` (o los comandos que uses con Vite).
